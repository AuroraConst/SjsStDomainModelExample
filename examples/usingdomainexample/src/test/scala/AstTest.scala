package org.aurora.extractors


import org.scalatest._
import wordspec._
import matchers.should._



class AstTest extends AnyWordSpec with Matchers {

  "pattern matching" should {
    "work like this" in {
    }

    /**
      *  In Scala 3.2, a pattern binding is only allowed if the pattern is irrefutable,
      *  that is, if the right-hand side's type conforms to the pattern's type
      */
    "value definition" in {
    }
  }
}
